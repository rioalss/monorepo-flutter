name: core_state
description: "Core state package for my apps"
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.16.9"

resolution: workspace

dependencies:
  core_domain:
  core_l10n:
  core_network:
  core_storage:
  design_system:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  flutter_riverpod: ^2.6.1
  get: ^4.6.6
  get_it: ^8.0.3
  injectable: ^2.5.0

dev_dependencies:
  build_runner: ^2.4.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.6.0
  very_good_analysis: ^10.2.0

flutter:
