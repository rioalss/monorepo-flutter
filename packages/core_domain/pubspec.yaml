name: core_domain
description: "Core domain package for my apps"
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.36.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.0
  flutter_test:
    sdk: flutter
  freezed: ^3.0.0
  json_serializable: ^6.9.0
  very_good_analysis: ^10.2.0

flutter:
